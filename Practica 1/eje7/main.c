#include <stdio.h>
#include <stdlib.h>

    void Conv (float orig[], size_t tamOrig, int final[], tamFinal);

int main()
{
    void Conv (float orig[], size_t tamOrig, int final[], tamFinal);
    printf("Hello world!\n");
    return 0;
}
